[pytest]
# Only tests/ holds real tests. test_prophet.py and archive/debug_scripts/test_*.py
# are manual probe scripts that talk to a running server, so keep pytest from
# collecting - and importing - them.
testpaths = tests
